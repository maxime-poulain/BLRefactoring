namespace TrainingHub.Translations;

/// <summary>
/// Marker for the boundary's refusal templates — the sentences a contract answers before any
/// domain rule is asked.
/// </summary>
/// <remarks>
/// The founding entry is the shape the house annotation attributes already write their messages
/// in, <c>{0}</c> placeholder included, so the day <c>AddDataAnnotationsLocalization</c> points
/// here nothing about the attributes has to move. See ADR 0088 for that deferred step.
/// </remarks>
public sealed class ValidationResources;
